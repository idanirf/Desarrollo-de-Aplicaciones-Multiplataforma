package `06-Colecciones`

/*
 * Los dataframe son inmutable.
 * Cuando pongamos DataSchema ya decimos que va con ese tipo de datos.
 * Instalación: con gradle e instalamos las dependencies necesarias. También es válido para Android
 * Pivotar -> transforma una columna en un nuevo grupo de columnas
 * dataFrameOf
 * Opciones (...) como si fuese sql.
 * sumarizingDouble
 * LetsPlot para dibujar gráficas
 */